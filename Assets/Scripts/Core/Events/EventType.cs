namespace Core.Events {
    public enum EventType {
        ToggleReady,
        OnWeaponSwap,
        OnWeaponFire,
        OnWeaponStop,
        OnWeaponReloadStart,
        OnWeaponReloadEnd,
    }
}